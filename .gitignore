# Secrets (runtime .env — .env.example is committed)
.env

# Compiled binaries
/airlock
/anchor
/agent-builder

# Go test outputs
*.test
*.out

# Proto codegen deps
/proto/node_modules/

# Caddy on-demand cert storage (bind-mounted into the dev container).
/caddy-certs/
# Cache created if a runtime command treats ${HOME} as a literal path.
/${HOME}/
